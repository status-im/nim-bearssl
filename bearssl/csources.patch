diff --git a/src/aead/eax.c b/src/aead/eax.c
index bcc704a..61063a6 100644
--- a/src/aead/eax.c
+++ b/src/aead/eax.c
@@ -504,22 +504,62 @@ br_eax_check_tag(br_eax_context *ctx, const void *tag)
 }
 
 /* see bearssl_aead.h */
+static void
+br_eax_reset_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	br_eax_reset((void *)a0, a1, a2);
+}
+
+static void
+br_eax_aad_inject_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	br_eax_aad_inject((void *)a0, a1, a2);
+}
+
+static void
+br_eax_flip_wrapper(const br_aead_class ** a0)
+{
+	br_eax_flip((void *)a0);
+}
+
+static void
+br_eax_run_wrapper(const br_aead_class ** a0, int a1, void * a2, size_t a3)
+{
+	br_eax_run((void *)a0, a1, a2, a3);
+}
+
+static void
+br_eax_get_tag_wrapper(const br_aead_class ** a0, void * a1)
+{
+	br_eax_get_tag((void *)a0, a1);
+}
+
+static uint32_t
+br_eax_check_tag_wrapper(const br_aead_class ** a0, const void * a1)
+{
+	return br_eax_check_tag((void *)a0, a1);
+}
+
+static void
+br_eax_get_tag_trunc_wrapper(const br_aead_class ** a0, void * a1, size_t a2)
+{
+	br_eax_get_tag_trunc((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_eax_check_tag_trunc_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	return br_eax_check_tag_trunc((void *)a0, a1, a2);
+}
+
 const br_aead_class br_eax_vtable = {
 	16,
-	(void (*)(const br_aead_class **, const void *, size_t))
-		&br_eax_reset,
-	(void (*)(const br_aead_class **, const void *, size_t))
-		&br_eax_aad_inject,
-	(void (*)(const br_aead_class **))
-		&br_eax_flip,
-	(void (*)(const br_aead_class **, int, void *, size_t))
-		&br_eax_run,
-	(void (*)(const br_aead_class **, void *))
-		&br_eax_get_tag,
-	(uint32_t (*)(const br_aead_class **, const void *))
-		&br_eax_check_tag,
-	(void (*)(const br_aead_class **, void *, size_t))
-		&br_eax_get_tag_trunc,
-	(uint32_t (*)(const br_aead_class **, const void *, size_t))
-		&br_eax_check_tag_trunc
+	&br_eax_reset_wrapper,
+	&br_eax_aad_inject_wrapper,
+	&br_eax_flip_wrapper,
+	&br_eax_run_wrapper,
+	&br_eax_get_tag_wrapper,
+	&br_eax_check_tag_wrapper,
+	&br_eax_get_tag_trunc_wrapper,
+	&br_eax_check_tag_trunc_wrapper
 };
diff --git a/src/aead/gcm.c b/src/aead/gcm.c
index ede5f08..1f83470 100644
--- a/src/aead/gcm.c
+++ b/src/aead/gcm.c
@@ -297,22 +297,62 @@ br_gcm_check_tag(br_gcm_context *ctx, const void *tag)
 }
 
 /* see bearssl_aead.h */
+static void
+br_gcm_reset_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	br_gcm_reset((void *)a0, a1, a2);
+}
+
+static void
+br_gcm_aad_inject_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	br_gcm_aad_inject((void *)a0, a1, a2);
+}
+
+static void
+br_gcm_flip_wrapper(const br_aead_class ** a0)
+{
+	br_gcm_flip((void *)a0);
+}
+
+static void
+br_gcm_run_wrapper(const br_aead_class ** a0, int a1, void * a2, size_t a3)
+{
+	br_gcm_run((void *)a0, a1, a2, a3);
+}
+
+static void
+br_gcm_get_tag_wrapper(const br_aead_class ** a0, void * a1)
+{
+	br_gcm_get_tag((void *)a0, a1);
+}
+
+static uint32_t
+br_gcm_check_tag_wrapper(const br_aead_class ** a0, const void * a1)
+{
+	return br_gcm_check_tag((void *)a0, a1);
+}
+
+static void
+br_gcm_get_tag_trunc_wrapper(const br_aead_class ** a0, void * a1, size_t a2)
+{
+	br_gcm_get_tag_trunc((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_gcm_check_tag_trunc_wrapper(const br_aead_class ** a0, const void * a1, size_t a2)
+{
+	return br_gcm_check_tag_trunc((void *)a0, a1, a2);
+}
+
 const br_aead_class br_gcm_vtable = {
 	16,
-	(void (*)(const br_aead_class **, const void *, size_t))
-		&br_gcm_reset,
-	(void (*)(const br_aead_class **, const void *, size_t))
-		&br_gcm_aad_inject,
-	(void (*)(const br_aead_class **))
-		&br_gcm_flip,
-	(void (*)(const br_aead_class **, int, void *, size_t))
-		&br_gcm_run,
-	(void (*)(const br_aead_class **, void *))
-		&br_gcm_get_tag,
-	(uint32_t (*)(const br_aead_class **, const void *))
-		&br_gcm_check_tag,
-	(void (*)(const br_aead_class **, void *, size_t))
-		&br_gcm_get_tag_trunc,
-	(uint32_t (*)(const br_aead_class **, const void *, size_t))
-		&br_gcm_check_tag_trunc
+	&br_gcm_reset_wrapper,
+	&br_gcm_aad_inject_wrapper,
+	&br_gcm_flip_wrapper,
+	&br_gcm_run_wrapper,
+	&br_gcm_get_tag_wrapper,
+	&br_gcm_check_tag_wrapper,
+	&br_gcm_get_tag_trunc_wrapper,
+	&br_gcm_check_tag_trunc_wrapper
 };
diff --git a/src/hash/md5.c b/src/hash/md5.c
index 0df7abe..d4d81fb 100644
--- a/src/hash/md5.c
+++ b/src/hash/md5.c
@@ -192,6 +192,36 @@ br_md5_set_state(br_md5_context *cc, const void *stb, uint64_t count)
 }
 
 /* see bearssl.h */
+static void
+br_md5_init_wrapper(const br_hash_class ** a0)
+{
+	br_md5_init((void *)a0);
+}
+
+static void
+br_md5_update_wrapper(const br_hash_class ** a0, const void * a1, size_t a2)
+{
+	br_md5_update((void *)a0, a1, a2);
+}
+
+static void
+br_md5_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_md5_out((void *)a0, a1);
+}
+
+static uint64_t
+br_md5_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_md5_state((void *)a0, a1);
+}
+
+static void
+br_md5_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_md5_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_md5_vtable = {
 	sizeof(br_md5_context),
 	BR_HASHDESC_ID(br_md5_ID)
@@ -199,10 +229,9 @@ const br_hash_class br_md5_vtable = {
 		| BR_HASHDESC_STATE(16)
 		| BR_HASHDESC_LBLEN(6)
 		| BR_HASHDESC_MD_PADDING,
-	(void (*)(const br_hash_class **))&br_md5_init,
-	(void (*)(const br_hash_class **, const void *, size_t))&br_md5_update,
-	(void (*)(const br_hash_class *const *, void *))&br_md5_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_md5_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_md5_set_state
+	&br_md5_init_wrapper,
+	&br_md5_update_wrapper,
+	&br_md5_out_wrapper,
+	&br_md5_state_wrapper,
+	&br_md5_set_state_wrapper
 };
diff --git a/src/hash/md5sha1.c b/src/hash/md5sha1.c
index f701aee..6794da5 100644
--- a/src/hash/md5sha1.c
+++ b/src/hash/md5sha1.c
@@ -123,19 +123,45 @@ br_md5sha1_set_state(br_md5sha1_context *cc, const void *stb, uint64_t count)
 }
 
 /* see bearssl.h */
+static void
+br_md5sha1_init_wrapper(const br_hash_class ** a0)
+{
+	br_md5sha1_init((void *)a0);
+}
+
+static void
+br_md5sha1_update_wrapper(const br_hash_class ** a0, const void * a1, size_t a2)
+{
+	br_md5sha1_update((void *)a0, a1, a2);
+}
+
+static void
+br_md5sha1_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_md5sha1_out((void *)a0, a1);
+}
+
+static uint64_t
+br_md5sha1_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_md5sha1_state((void *)a0, a1);
+}
+
+static void
+br_md5sha1_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_md5sha1_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_md5sha1_vtable = {
 	sizeof(br_md5sha1_context),
 	BR_HASHDESC_ID(br_md5sha1_ID)
 		| BR_HASHDESC_OUT(36)
 		| BR_HASHDESC_STATE(36)
 		| BR_HASHDESC_LBLEN(6),
-	(void (*)(const br_hash_class **))&br_md5sha1_init,
-	(void (*)(const br_hash_class **, const void *, size_t))
-		&br_md5sha1_update,
-	(void (*)(const br_hash_class *const *, void *))
-		&br_md5sha1_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))
-		&br_md5sha1_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_md5sha1_set_state
+	&br_md5sha1_init_wrapper,
+	&br_md5sha1_update_wrapper,
+	&br_md5sha1_out_wrapper,
+	&br_md5sha1_state_wrapper,
+	&br_md5sha1_set_state_wrapper
 };
diff --git a/src/hash/sha1.c b/src/hash/sha1.c
index 4f65d84..6783e30 100644
--- a/src/hash/sha1.c
+++ b/src/hash/sha1.c
@@ -174,6 +174,36 @@ br_sha1_set_state(br_sha1_context *cc, const void *stb, uint64_t count)
 }
 
 /* see bearssl.h */
+static void
+br_sha1_init_wrapper(const br_hash_class ** a0)
+{
+	br_sha1_init((void *)a0);
+}
+
+static void
+br_sha1_update_wrapper(const br_hash_class ** a0, const void * a1, size_t a2)
+{
+	br_sha1_update((void *)a0, a1, a2);
+}
+
+static void
+br_sha1_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_sha1_out((void *)a0, a1);
+}
+
+static uint64_t
+br_sha1_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_sha1_state((void *)a0, a1);
+}
+
+static void
+br_sha1_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_sha1_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_sha1_vtable = {
 	sizeof(br_sha1_context),
 	BR_HASHDESC_ID(br_sha1_ID)
@@ -182,10 +212,9 @@ const br_hash_class br_sha1_vtable = {
 		| BR_HASHDESC_LBLEN(6)
 		| BR_HASHDESC_MD_PADDING
 		| BR_HASHDESC_MD_PADDING_BE,
-	(void (*)(const br_hash_class **))&br_sha1_init,
-	(void (*)(const br_hash_class **, const void *, size_t))&br_sha1_update,
-	(void (*)(const br_hash_class *const *, void *))&br_sha1_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_sha1_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_sha1_set_state
+	&br_sha1_init_wrapper,
+	&br_sha1_update_wrapper,
+	&br_sha1_out_wrapper,
+	&br_sha1_state_wrapper,
+	&br_sha1_set_state_wrapper
 };
diff --git a/src/hash/sha2big.c b/src/hash/sha2big.c
index 5be92ed..3f9b7e9 100644
--- a/src/hash/sha2big.c
+++ b/src/hash/sha2big.c
@@ -247,6 +247,36 @@ br_sha512_out(const br_sha512_context *cc, void *dst)
 }
 
 /* see bearssl.h */
+static void
+br_sha384_init_wrapper(const br_hash_class ** a0)
+{
+	br_sha384_init((void *)a0);
+}
+
+static void
+br_sha384_update_wrapper(const br_hash_class ** a0, const void * a1, size_t a2)
+{
+	br_sha384_update((void *)a0, a1, a2);
+}
+
+static void
+br_sha384_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_sha384_out((void *)a0, a1);
+}
+
+static uint64_t
+br_sha384_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_sha384_state((void *)a0, a1);
+}
+
+static void
+br_sha384_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_sha384_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_sha384_vtable = {
 	sizeof(br_sha384_context),
 	BR_HASHDESC_ID(br_sha384_ID)
@@ -256,16 +286,44 @@ const br_hash_class br_sha384_vtable = {
 		| BR_HASHDESC_MD_PADDING
 		| BR_HASHDESC_MD_PADDING_BE
 		| BR_HASHDESC_MD_PADDING_128,
-	(void (*)(const br_hash_class **))&br_sha384_init,
-	(void (*)(const br_hash_class **, const void *, size_t))
-		&br_sha384_update,
-	(void (*)(const br_hash_class *const *, void *))&br_sha384_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_sha384_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_sha384_set_state
+	&br_sha384_init_wrapper,
+	&br_sha384_update_wrapper,
+	&br_sha384_out_wrapper,
+	&br_sha384_state_wrapper,
+	&br_sha384_set_state_wrapper
 };
 
 /* see bearssl.h */
+static void
+br_sha512_init_wrapper(const br_hash_class ** a0)
+{
+	br_sha512_init((void *)a0);
+}
+
+static void
+br_sha512_update_wrapper(const br_hash_class ** a0, const void * a1, size_t a2)
+{
+	br_sha512_update((void *)a0, a1, a2);
+}
+
+static void
+br_sha512_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_sha512_out((void *)a0, a1);
+}
+
+static uint64_t
+br_sha512_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_sha512_state((void *)a0, a1);
+}
+
+static void
+br_sha512_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_sha512_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_sha512_vtable = {
 	sizeof(br_sha512_context),
 	BR_HASHDESC_ID(br_sha512_ID)
@@ -275,11 +333,9 @@ const br_hash_class br_sha512_vtable = {
 		| BR_HASHDESC_MD_PADDING
 		| BR_HASHDESC_MD_PADDING_BE
 		| BR_HASHDESC_MD_PADDING_128,
-	(void (*)(const br_hash_class **))&br_sha512_init,
-	(void (*)(const br_hash_class **, const void *, size_t))
-		&br_sha512_update,
-	(void (*)(const br_hash_class *const *, void *))&br_sha512_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_sha512_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_sha512_set_state
+	&br_sha512_init_wrapper,
+	&br_sha512_update_wrapper,
+	&br_sha512_out_wrapper,
+	&br_sha512_state_wrapper,
+	&br_sha512_set_state_wrapper
 };
diff --git a/src/hash/sha2small.c b/src/hash/sha2small.c
index ca19655..eaf8ac5 100644
--- a/src/hash/sha2small.c
+++ b/src/hash/sha2small.c
@@ -305,6 +305,37 @@ br_sha256_out(const br_sha256_context *cc, void *dst)
 }
 
 /* see bearssl.h */
+static void
+br_sha224_init_wrapper(const br_hash_class ** a0)
+{
+	br_sha224_init((void *)a0);
+}
+
+static void
+br_sha224_update_wrapper(const br_hash_class ** a0,
+		const void * a1, size_t a2)
+{
+	br_sha224_update((void *)a0, a1, a2);
+}
+
+static void
+br_sha224_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_sha224_out((void *)a0, a1);
+}
+
+static uint64_t
+br_sha224_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_sha224_state((void *)a0, a1);
+}
+
+static void
+br_sha224_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_sha224_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_sha224_vtable = {
 	sizeof(br_sha224_context),
 	BR_HASHDESC_ID(br_sha224_ID)
@@ -313,16 +344,45 @@ const br_hash_class br_sha224_vtable = {
 		| BR_HASHDESC_LBLEN(6)
 		| BR_HASHDESC_MD_PADDING
 		| BR_HASHDESC_MD_PADDING_BE,
-	(void (*)(const br_hash_class **))&br_sha224_init,
-	(void (*)(const br_hash_class **,
-		const void *, size_t))&br_sha224_update,
-	(void (*)(const br_hash_class *const *, void *))&br_sha224_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_sha224_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_sha224_set_state
+	&br_sha224_init_wrapper,
+	&br_sha224_update_wrapper,
+	&br_sha224_out_wrapper,
+	&br_sha224_state_wrapper,
+	&br_sha224_set_state_wrapper
 };
 
 /* see bearssl.h */
+static void
+br_sha256_init_wrapper(const br_hash_class ** a0)
+{
+	br_sha256_init((void *)a0);
+}
+
+static void
+br_sha256_update_wrapper(const br_hash_class ** a0,
+		const void * a1, size_t a2)
+{
+	br_sha256_update((void *)a0, a1, a2);
+}
+
+static void
+br_sha256_out_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	br_sha256_out((void *)a0, a1);
+}
+
+static uint64_t
+br_sha256_state_wrapper(const br_hash_class *const * a0, void * a1)
+{
+	return br_sha256_state((void *)a0, a1);
+}
+
+static void
+br_sha256_set_state_wrapper(const br_hash_class ** a0, const void * a1, uint64_t a2)
+{
+	br_sha256_set_state((void *)a0, a1, a2);
+}
+
 const br_hash_class br_sha256_vtable = {
 	sizeof(br_sha256_context),
 	BR_HASHDESC_ID(br_sha256_ID)
@@ -331,11 +391,9 @@ const br_hash_class br_sha256_vtable = {
 		| BR_HASHDESC_LBLEN(6)
 		| BR_HASHDESC_MD_PADDING
 		| BR_HASHDESC_MD_PADDING_BE,
-	(void (*)(const br_hash_class **))&br_sha256_init,
-	(void (*)(const br_hash_class **,
-		const void *, size_t))&br_sha256_update,
-	(void (*)(const br_hash_class *const *, void *))&br_sha256_out,
-	(uint64_t (*)(const br_hash_class *const *, void *))&br_sha256_state,
-	(void (*)(const br_hash_class **, const void *, uint64_t))
-		&br_sha256_set_state
+	&br_sha256_init_wrapper,
+	&br_sha256_update_wrapper,
+	&br_sha256_out_wrapper,
+	&br_sha256_state_wrapper,
+	&br_sha256_set_state_wrapper
 };
diff --git a/src/inner.h b/src/inner.h
index 07e1d0a..20e5e75 100644
--- a/src/inner.h
+++ b/src/inner.h
@@ -488,7 +488,8 @@ static inline void
 br_enc16le(void *dst, unsigned x)
 {
 #if BR_LE_UNALIGNED
-	((br_union_u16 *)dst)->u = x;
+	uint16_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -502,7 +503,8 @@ static inline void
 br_enc16be(void *dst, unsigned x)
 {
 #if BR_BE_UNALIGNED
-	((br_union_u16 *)dst)->u = x;
+	uint16_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -516,7 +518,9 @@ static inline unsigned
 br_dec16le(const void *src)
 {
 #if BR_LE_UNALIGNED
-	return ((const br_union_u16 *)src)->u;
+	uint16_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -529,7 +533,9 @@ static inline unsigned
 br_dec16be(const void *src)
 {
 #if BR_BE_UNALIGNED
-	return ((const br_union_u16 *)src)->u;
+	uint16_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -542,7 +548,8 @@ static inline void
 br_enc32le(void *dst, uint32_t x)
 {
 #if BR_LE_UNALIGNED
-	((br_union_u32 *)dst)->u = x;
+	uint32_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -558,7 +565,8 @@ static inline void
 br_enc32be(void *dst, uint32_t x)
 {
 #if BR_BE_UNALIGNED
-	((br_union_u32 *)dst)->u = x;
+	uint32_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -574,7 +582,9 @@ static inline uint32_t
 br_dec32le(const void *src)
 {
 #if BR_LE_UNALIGNED
-	return ((const br_union_u32 *)src)->u;
+	uint32_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -590,7 +600,9 @@ static inline uint32_t
 br_dec32be(const void *src)
 {
 #if BR_BE_UNALIGNED
-	return ((const br_union_u32 *)src)->u;
+	uint32_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -606,7 +618,8 @@ static inline void
 br_enc64le(void *dst, uint64_t x)
 {
 #if BR_LE_UNALIGNED
-	((br_union_u64 *)dst)->u = x;
+	uint64_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -620,7 +633,8 @@ static inline void
 br_enc64be(void *dst, uint64_t x)
 {
 #if BR_BE_UNALIGNED
-	((br_union_u64 *)dst)->u = x;
+	uint64_t v = x;
+	memcpy(dst, &v, sizeof v);
 #else
 	unsigned char *buf;
 
@@ -634,7 +648,9 @@ static inline uint64_t
 br_dec64le(const void *src)
 {
 #if BR_LE_UNALIGNED
-	return ((const br_union_u64 *)src)->u;
+	uint64_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -648,7 +664,9 @@ static inline uint64_t
 br_dec64be(const void *src)
 {
 #if BR_BE_UNALIGNED
-	return ((const br_union_u64 *)src)->u;
+	uint64_t v;
+	memcpy(&v, src, sizeof v);
+	return v;
 #else
 	const unsigned char *buf;
 
@@ -2575,3 +2593,4 @@ br_cpuid(uint32_t mask_eax, uint32_t mask_ebx,
 /* ==================================================================== */
 
 #endif
+/* nim-bearssl patches applied - 2026-09-24 */
diff --git a/src/rand/aesctr_drbg.c b/src/rand/aesctr_drbg.c
index 8dbd501..a9401a7 100644
--- a/src/rand/aesctr_drbg.c
+++ b/src/rand/aesctr_drbg.c
@@ -195,12 +195,27 @@ br_aesctr_drbg_update(br_aesctr_drbg_context *ctx, const void *seed, size_t len)
 }
 
 /* see bearssl_rand.h */
+static void
+br_aesctr_drbg_init_wrapper(const br_prng_class ** a0, const void * a1, const void * a2, size_t a3)
+{
+	br_aesctr_drbg_init((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aesctr_drbg_generate_wrapper(const br_prng_class ** a0, void * a1, size_t a2)
+{
+	br_aesctr_drbg_generate((void *)a0, a1, a2);
+}
+
+static void
+br_aesctr_drbg_update_wrapper(const br_prng_class ** a0, const void * a1, size_t a2)
+{
+	br_aesctr_drbg_update((void *)a0, a1, a2);
+}
+
 const br_prng_class br_aesctr_drbg_vtable = {
 	sizeof(br_aesctr_drbg_context),
-	(void (*)(const br_prng_class **, const void *, const void *, size_t))
-		&br_aesctr_drbg_init,
-	(void (*)(const br_prng_class **, void *, size_t))
-		&br_aesctr_drbg_generate,
-	(void (*)(const br_prng_class **, const void *, size_t))
-		&br_aesctr_drbg_update
+	&br_aesctr_drbg_init_wrapper,
+	&br_aesctr_drbg_generate_wrapper,
+	&br_aesctr_drbg_update_wrapper
 };
diff --git a/src/rand/hmac_drbg.c b/src/rand/hmac_drbg.c
index d746756..30e13b3 100644
--- a/src/rand/hmac_drbg.c
+++ b/src/rand/hmac_drbg.c
@@ -146,12 +146,27 @@ br_hmac_drbg_update(br_hmac_drbg_context *ctx, const void *seed, size_t len)
 }
 
 /* see bearssl.h */
+static void
+br_hmac_drbg_init_wrapper(const br_prng_class ** a0, const void * a1, const void * a2, size_t a3)
+{
+	br_hmac_drbg_init((void *)a0, a1, a2, a3);
+}
+
+static void
+br_hmac_drbg_generate_wrapper(const br_prng_class ** a0, void * a1, size_t a2)
+{
+	br_hmac_drbg_generate((void *)a0, a1, a2);
+}
+
+static void
+br_hmac_drbg_update_wrapper(const br_prng_class ** a0, const void * a1, size_t a2)
+{
+	br_hmac_drbg_update((void *)a0, a1, a2);
+}
+
 const br_prng_class br_hmac_drbg_vtable = {
 	sizeof(br_hmac_drbg_context),
-	(void (*)(const br_prng_class **, const void *, const void *, size_t))
-		&br_hmac_drbg_init,
-	(void (*)(const br_prng_class **, void *, size_t))
-		&br_hmac_drbg_generate,
-	(void (*)(const br_prng_class **, const void *, size_t))
-		&br_hmac_drbg_update
+	&br_hmac_drbg_init_wrapper,
+	&br_hmac_drbg_generate_wrapper,
+	&br_hmac_drbg_update_wrapper
 };
diff --git a/src/ssl/ssl_engine.c b/src/ssl/ssl_engine.c
index f59fe1a..3b9d3d4 100644
--- a/src/ssl/ssl_engine.c
+++ b/src/ssl/ssl_engine.c
@@ -956,13 +956,23 @@ clear_encrypt(br_sslrec_out_clear_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static void
+clear_max_plaintext_wrapper(const br_sslrec_out_class *const * a0, size_t * a1, size_t * a2)
+{
+	clear_max_plaintext((void *)a0, a1, a2);
+}
+
+static unsigned char *
+clear_encrypt_wrapper(const br_sslrec_out_class ** a0,
+		int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return clear_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
 const br_sslrec_out_class br_sslrec_out_clear_vtable = {
 	sizeof(br_sslrec_out_clear_context),
-	(void (*)(const br_sslrec_out_class *const *, size_t *, size_t *))
-		&clear_max_plaintext,
-	(unsigned char *(*)(const br_sslrec_out_class **,
-		int, unsigned, void *, size_t *))
-		&clear_encrypt
+	&clear_max_plaintext_wrapper,
+	&clear_encrypt_wrapper
 };
 
 /* ==================================================================== */
diff --git a/src/ssl/ssl_rec_cbc.c b/src/ssl/ssl_rec_cbc.c
index 9e89bb6..fa8474c 100644
--- a/src/ssl/ssl_rec_cbc.c
+++ b/src/ssl/ssl_rec_cbc.c
@@ -230,20 +230,35 @@ cbc_decrypt(br_sslrec_in_cbc_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static int
+cbc_check_length_wrapper(const br_sslrec_in_class *const * a0, size_t a1)
+{
+	return cbc_check_length((void *)a0, a1);
+}
+
+static unsigned char *
+cbc_decrypt_wrapper(const br_sslrec_in_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return cbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+in_cbc_init_wrapper(const br_sslrec_in_cbc_class ** a0,
+		const br_block_cbcdec_class * a1, const void * a2, size_t a3,
+		const br_hash_class * a4, const void * a5, size_t a6, size_t a7,
+		const void * a8)
+{
+	in_cbc_init((void *)a0, a1, a2, a3, a4, a5, a6, a7, a8);
+}
+
 const br_sslrec_in_cbc_class br_sslrec_in_cbc_vtable = {
 	{
 		sizeof(br_sslrec_in_cbc_context),
-		(int (*)(const br_sslrec_in_class *const *, size_t))
-			&cbc_check_length,
-		(unsigned char *(*)(const br_sslrec_in_class **,
-			int, unsigned, void *, size_t *))
-			&cbc_decrypt
+		&cbc_check_length_wrapper,
+		&cbc_decrypt_wrapper
 	},
-	(void (*)(const br_sslrec_in_cbc_class **,
-		const br_block_cbcdec_class *, const void *, size_t,
-		const br_hash_class *, const void *, size_t, size_t,
-		const void *))
-		&in_cbc_init
+	&in_cbc_init_wrapper
 };
 
 /*
@@ -422,19 +437,34 @@ cbc_encrypt(br_sslrec_out_cbc_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static void
+cbc_max_plaintext_wrapper(const br_sslrec_out_class *const * a0,
+			size_t * a1, size_t * a2)
+{
+	cbc_max_plaintext((void *)a0, a1, a2);
+}
+
+static unsigned char *
+cbc_encrypt_wrapper(const br_sslrec_out_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return cbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+out_cbc_init_wrapper(const br_sslrec_out_cbc_class ** a0,
+		const br_block_cbcenc_class * a1, const void * a2, size_t a3,
+		const br_hash_class * a4, const void * a5, size_t a6, size_t a7,
+		const void * a8)
+{
+	out_cbc_init((void *)a0, a1, a2, a3, a4, a5, a6, a7, a8);
+}
+
 const br_sslrec_out_cbc_class br_sslrec_out_cbc_vtable = {
 	{
 		sizeof(br_sslrec_out_cbc_context),
-		(void (*)(const br_sslrec_out_class *const *,
-			size_t *, size_t *))
-			&cbc_max_plaintext,
-		(unsigned char *(*)(const br_sslrec_out_class **,
-			int, unsigned, void *, size_t *))
-			&cbc_encrypt
+		&cbc_max_plaintext_wrapper,
+		&cbc_encrypt_wrapper
 	},
-	(void (*)(const br_sslrec_out_cbc_class **,
-		const br_block_cbcenc_class *, const void *, size_t,
-		const br_hash_class *, const void *, size_t, size_t,
-		const void *))
-		&out_cbc_init
+	&out_cbc_init_wrapper
 };
diff --git a/src/ssl/ssl_rec_ccm.c b/src/ssl/ssl_rec_ccm.c
index 92c3295..170ed15 100644
--- a/src/ssl/ssl_rec_ccm.c
+++ b/src/ssl/ssl_rec_ccm.c
@@ -106,19 +106,34 @@ ccm_decrypt(br_sslrec_ccm_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static int
+ccm_check_length_wrapper(const br_sslrec_in_class *const * a0, size_t a1)
+{
+	return ccm_check_length((void *)a0, a1);
+}
+
+static unsigned char *
+ccm_decrypt_wrapper(const br_sslrec_in_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return ccm_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+in_ccm_init_wrapper(const br_sslrec_in_ccm_class ** a0,
+		const br_block_ctrcbc_class * a1, const void * a2, size_t a3,
+		const void * a4, size_t a5)
+{
+	in_ccm_init((void *)a0, a1, a2, a3, a4, a5);
+}
+
 const br_sslrec_in_ccm_class br_sslrec_in_ccm_vtable = {
 	{
 		sizeof(br_sslrec_ccm_context),
-		(int (*)(const br_sslrec_in_class *const *, size_t))
-			&ccm_check_length,
-		(unsigned char *(*)(const br_sslrec_in_class **,
-			int, unsigned, void *, size_t *))
-			&ccm_decrypt
+		&ccm_check_length_wrapper,
+		&ccm_decrypt_wrapper
 	},
-	(void (*)(const br_sslrec_in_ccm_class **,
-		const br_block_ctrcbc_class *, const void *, size_t,
-		const void *, size_t))
-		&in_ccm_init
+	&in_ccm_init_wrapper
 };
 
 static void
@@ -196,18 +211,33 @@ ccm_encrypt(br_sslrec_ccm_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static void
+ccm_max_plaintext_wrapper(const br_sslrec_out_class *const * a0,
+			size_t * a1, size_t * a2)
+{
+	ccm_max_plaintext((void *)a0, a1, a2);
+}
+
+static unsigned char *
+ccm_encrypt_wrapper(const br_sslrec_out_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return ccm_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+out_ccm_init_wrapper(const br_sslrec_out_ccm_class ** a0,
+		const br_block_ctrcbc_class * a1, const void * a2, size_t a3,
+		const void * a4, size_t a5)
+{
+	out_ccm_init((void *)a0, a1, a2, a3, a4, a5);
+}
+
 const br_sslrec_out_ccm_class br_sslrec_out_ccm_vtable = {
 	{
 		sizeof(br_sslrec_ccm_context),
-		(void (*)(const br_sslrec_out_class *const *,
-			size_t *, size_t *))
-			&ccm_max_plaintext,
-		(unsigned char *(*)(const br_sslrec_out_class **,
-			int, unsigned, void *, size_t *))
-			&ccm_encrypt
+		&ccm_max_plaintext_wrapper,
+		&ccm_encrypt_wrapper
 	},
-	(void (*)(const br_sslrec_out_ccm_class **,
-		const br_block_ctrcbc_class *, const void *, size_t,
-		const void *, size_t))
-		&out_ccm_init
+	&out_ccm_init_wrapper
 };
diff --git a/src/ssl/ssl_rec_chapol.c b/src/ssl/ssl_rec_chapol.c
index 73b3c78..2881f43 100644
--- a/src/ssl/ssl_rec_chapol.c
+++ b/src/ssl/ssl_rec_chapol.c
@@ -103,19 +103,34 @@ chapol_decrypt(br_sslrec_chapol_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static int
+chapol_check_length_wrapper(const br_sslrec_in_class *const * a0, size_t a1)
+{
+	return chapol_check_length((void *)a0, a1);
+}
+
+static unsigned char *
+chapol_decrypt_wrapper(const br_sslrec_in_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return chapol_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+in_chapol_init_wrapper(const br_sslrec_in_chapol_class ** a0,
+		br_chacha20_run a1, br_poly1305_run a2,
+		const void * a3, const void * a4)
+{
+	in_chapol_init((void *)a0, a1, a2, a3, a4);
+}
+
 const br_sslrec_in_chapol_class br_sslrec_in_chapol_vtable = {
 	{
 		sizeof(br_sslrec_chapol_context),
-		(int (*)(const br_sslrec_in_class *const *, size_t))
-			&chapol_check_length,
-		(unsigned char *(*)(const br_sslrec_in_class **,
-			int, unsigned, void *, size_t *))
-			&chapol_decrypt
+		&chapol_check_length_wrapper,
+		&chapol_decrypt_wrapper
 	},
-	(void (*)(const br_sslrec_in_chapol_class **,
-		br_chacha20_run, br_poly1305_run,
-		const void *, const void *))
-		&in_chapol_init
+	&in_chapol_init_wrapper
 };
 
 static void
@@ -160,18 +175,33 @@ chapol_encrypt(br_sslrec_chapol_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static void
+chapol_max_plaintext_wrapper(const br_sslrec_out_class *const * a0,
+			size_t * a1, size_t * a2)
+{
+	chapol_max_plaintext((void *)a0, a1, a2);
+}
+
+static unsigned char *
+chapol_encrypt_wrapper(const br_sslrec_out_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return chapol_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+out_chapol_init_wrapper(const br_sslrec_out_chapol_class ** a0,
+		br_chacha20_run a1, br_poly1305_run a2,
+		const void * a3, const void * a4)
+{
+	out_chapol_init((void *)a0, a1, a2, a3, a4);
+}
+
 const br_sslrec_out_chapol_class br_sslrec_out_chapol_vtable = {
 	{
 		sizeof(br_sslrec_chapol_context),
-		(void (*)(const br_sslrec_out_class *const *,
-			size_t *, size_t *))
-			&chapol_max_plaintext,
-		(unsigned char *(*)(const br_sslrec_out_class **,
-			int, unsigned, void *, size_t *))
-			&chapol_encrypt
+		&chapol_max_plaintext_wrapper,
+		&chapol_encrypt_wrapper
 	},
-	(void (*)(const br_sslrec_out_chapol_class **,
-		br_chacha20_run, br_poly1305_run,
-		const void *, const void *))
-		&out_chapol_init
+	&out_chapol_init_wrapper
 };
diff --git a/src/ssl/ssl_rec_gcm.c b/src/ssl/ssl_rec_gcm.c
index 70df277..eece1ea 100644
--- a/src/ssl/ssl_rec_gcm.c
+++ b/src/ssl/ssl_rec_gcm.c
@@ -150,19 +150,34 @@ gcm_decrypt(br_sslrec_gcm_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static int
+gcm_check_length_wrapper(const br_sslrec_in_class *const * a0, size_t a1)
+{
+	return gcm_check_length((void *)a0, a1);
+}
+
+static unsigned char *
+gcm_decrypt_wrapper(const br_sslrec_in_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return gcm_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+in_gcm_init_wrapper(const br_sslrec_in_gcm_class ** a0,
+		const br_block_ctr_class * a1, const void * a2, size_t a3,
+		br_ghash a4, const void * a5)
+{
+	in_gcm_init((void *)a0, a1, a2, a3, a4, a5);
+}
+
 const br_sslrec_in_gcm_class br_sslrec_in_gcm_vtable = {
 	{
 		sizeof(br_sslrec_gcm_context),
-		(int (*)(const br_sslrec_in_class *const *, size_t))
-			&gcm_check_length,
-		(unsigned char *(*)(const br_sslrec_in_class **,
-			int, unsigned, void *, size_t *))
-			&gcm_decrypt
+		&gcm_check_length_wrapper,
+		&gcm_decrypt_wrapper
 	},
-	(void (*)(const br_sslrec_in_gcm_class **,
-		const br_block_ctr_class *, const void *, size_t,
-		br_ghash, const void *))
-		&in_gcm_init
+	&in_gcm_init_wrapper
 };
 
 static void
@@ -218,18 +233,33 @@ gcm_encrypt(br_sslrec_gcm_context *cc,
 }
 
 /* see bearssl_ssl.h */
+static void
+gcm_max_plaintext_wrapper(const br_sslrec_out_class *const * a0,
+			size_t * a1, size_t * a2)
+{
+	gcm_max_plaintext((void *)a0, a1, a2);
+}
+
+static unsigned char *
+gcm_encrypt_wrapper(const br_sslrec_out_class ** a0,
+			int a1, unsigned a2, void * a3, size_t * a4)
+{
+	return gcm_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+out_gcm_init_wrapper(const br_sslrec_out_gcm_class ** a0,
+		const br_block_ctr_class * a1, const void * a2, size_t a3,
+		br_ghash a4, const void * a5)
+{
+	out_gcm_init((void *)a0, a1, a2, a3, a4, a5);
+}
+
 const br_sslrec_out_gcm_class br_sslrec_out_gcm_vtable = {
 	{
 		sizeof(br_sslrec_gcm_context),
-		(void (*)(const br_sslrec_out_class *const *,
-			size_t *, size_t *))
-			&gcm_max_plaintext,
-		(unsigned char *(*)(const br_sslrec_out_class **,
-			int, unsigned, void *, size_t *))
-			&gcm_encrypt
+		&gcm_max_plaintext_wrapper,
+		&gcm_encrypt_wrapper
 	},
-	(void (*)(const br_sslrec_out_gcm_class **,
-		const br_block_ctr_class *, const void *, size_t,
-		br_ghash, const void *))
-		&out_gcm_init
+	&out_gcm_init_wrapper
 };
diff --git a/src/symcipher/aes_big_cbcdec.c b/src/symcipher/aes_big_cbcdec.c
index d969a3b..fd55c94 100644
--- a/src/symcipher/aes_big_cbcdec.c
+++ b/src/symcipher/aes_big_cbcdec.c
@@ -58,12 +58,22 @@ br_aes_big_cbcdec_run(const br_aes_big_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_big_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_big_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_big_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_big_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_big_cbcdec_vtable = {
 	sizeof(br_aes_big_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_big_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_big_cbcdec_run
+	&br_aes_big_cbcdec_init_wrapper,
+	&br_aes_big_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/aes_big_cbcenc.c b/src/symcipher/aes_big_cbcenc.c
index 265e53b..78fa469 100644
--- a/src/symcipher/aes_big_cbcenc.c
+++ b/src/symcipher/aes_big_cbcenc.c
@@ -56,12 +56,22 @@ br_aes_big_cbcenc_run(const br_aes_big_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_big_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_big_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_big_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_big_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_big_cbcenc_vtable = {
 	sizeof(br_aes_big_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_big_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_big_cbcenc_run
+	&br_aes_big_cbcenc_init_wrapper,
+	&br_aes_big_cbcenc_run_wrapper
 };
diff --git a/src/symcipher/aes_big_ctr.c b/src/symcipher/aes_big_ctr.c
index 18fbb84..8163b22 100644
--- a/src/symcipher/aes_big_ctr.c
+++ b/src/symcipher/aes_big_ctr.c
@@ -72,13 +72,23 @@ br_aes_big_ctr_run(const br_aes_big_ctr_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_big_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_big_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_big_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_big_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_big_ctr_vtable = {
 	sizeof(br_aes_big_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_big_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_big_ctr_run
+	&br_aes_big_ctr_init_wrapper,
+	&br_aes_big_ctr_run_wrapper
 };
diff --git a/src/symcipher/aes_big_ctrcbc.c b/src/symcipher/aes_big_ctrcbc.c
index d45ca76..b098a83 100644
--- a/src/symcipher/aes_big_ctrcbc.c
+++ b/src/symcipher/aes_big_ctrcbc.c
@@ -121,22 +121,47 @@ br_aes_big_ctrcbc_decrypt(const br_aes_big_ctrcbc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_big_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_big_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_big_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_big_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_big_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_big_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_big_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_big_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_big_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_big_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_big_ctrcbc_vtable = {
 	sizeof(br_aes_big_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_big_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_big_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_big_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_big_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_big_ctrcbc_mac
+	&br_aes_big_ctrcbc_init_wrapper,
+	&br_aes_big_ctrcbc_encrypt_wrapper,
+	&br_aes_big_ctrcbc_decrypt_wrapper,
+	&br_aes_big_ctrcbc_ctr_wrapper,
+	&br_aes_big_ctrcbc_mac_wrapper
 };
diff --git a/src/symcipher/aes_ct64_cbcdec.c b/src/symcipher/aes_ct64_cbcdec.c
index 5a7360b..bdf0479 100644
--- a/src/symcipher/aes_ct64_cbcdec.c
+++ b/src/symcipher/aes_ct64_cbcdec.c
@@ -93,12 +93,22 @@ br_aes_ct64_cbcdec_run(const br_aes_ct64_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct64_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct64_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct64_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_ct64_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_ct64_cbcdec_vtable = {
 	sizeof(br_aes_ct64_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_ct64_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_ct64_cbcdec_run
+	&br_aes_ct64_cbcdec_init_wrapper,
+	&br_aes_ct64_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/aes_ct64_cbcenc.c b/src/symcipher/aes_ct64_cbcenc.c
index 6cb9dec..5b6abaa 100644
--- a/src/symcipher/aes_ct64_cbcenc.c
+++ b/src/symcipher/aes_ct64_cbcenc.c
@@ -70,12 +70,22 @@ br_aes_ct64_cbcenc_run(const br_aes_ct64_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct64_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct64_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct64_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_ct64_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_ct64_cbcenc_vtable = {
 	sizeof(br_aes_ct64_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_ct64_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_ct64_cbcenc_run
+	&br_aes_ct64_cbcenc_init_wrapper,
+	&br_aes_ct64_cbcenc_run_wrapper
 };
diff --git a/src/symcipher/aes_ct64_ctr.c b/src/symcipher/aes_ct64_ctr.c
index 1275873..daf66a6 100644
--- a/src/symcipher/aes_ct64_ctr.c
+++ b/src/symcipher/aes_ct64_ctr.c
@@ -102,13 +102,23 @@ br_aes_ct64_ctr_run(const br_aes_ct64_ctr_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct64_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct64_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_ct64_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_ct64_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_ct64_ctr_vtable = {
 	sizeof(br_aes_ct64_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_ct64_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_ct64_ctr_run
+	&br_aes_ct64_ctr_init_wrapper,
+	&br_aes_ct64_ctr_run_wrapper
 };
diff --git a/src/symcipher/aes_ct64_ctrcbc.c b/src/symcipher/aes_ct64_ctrcbc.c
index 21bb8ef..f60a68c 100644
--- a/src/symcipher/aes_ct64_ctrcbc.c
+++ b/src/symcipher/aes_ct64_ctrcbc.c
@@ -412,22 +412,47 @@ br_aes_ct64_ctrcbc_decrypt(const br_aes_ct64_ctrcbc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct64_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct64_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct64_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_ct64_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_ct64_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_ct64_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_ct64_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_ct64_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_ct64_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_ct64_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_ct64_ctrcbc_vtable = {
 	sizeof(br_aes_ct64_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_ct64_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_ct64_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_ct64_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_ct64_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_ct64_ctrcbc_mac
+	&br_aes_ct64_ctrcbc_init_wrapper,
+	&br_aes_ct64_ctrcbc_encrypt_wrapper,
+	&br_aes_ct64_ctrcbc_decrypt_wrapper,
+	&br_aes_ct64_ctrcbc_ctr_wrapper,
+	&br_aes_ct64_ctrcbc_mac_wrapper
 };
diff --git a/src/symcipher/aes_ct_cbcdec.c b/src/symcipher/aes_ct_cbcdec.c
index 522645a..ddb0c86 100644
--- a/src/symcipher/aes_ct_cbcdec.c
+++ b/src/symcipher/aes_ct_cbcdec.c
@@ -100,12 +100,22 @@ br_aes_ct_cbcdec_run(const br_aes_ct_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_ct_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_ct_cbcdec_vtable = {
 	sizeof(br_aes_ct_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_ct_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_ct_cbcdec_run
+	&br_aes_ct_cbcdec_init_wrapper,
+	&br_aes_ct_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/aes_ct_cbcenc.c b/src/symcipher/aes_ct_cbcenc.c
index cb85977..83fabbf 100644
--- a/src/symcipher/aes_ct_cbcenc.c
+++ b/src/symcipher/aes_ct_cbcenc.c
@@ -80,12 +80,22 @@ br_aes_ct_cbcenc_run(const br_aes_ct_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_ct_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_ct_cbcenc_vtable = {
 	sizeof(br_aes_ct_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_ct_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_ct_cbcenc_run
+	&br_aes_ct_cbcenc_init_wrapper,
+	&br_aes_ct_cbcenc_run_wrapper
 };
diff --git a/src/symcipher/aes_ct_ctr.c b/src/symcipher/aes_ct_ctr.c
index f407689..c8e49ba 100644
--- a/src/symcipher/aes_ct_ctr.c
+++ b/src/symcipher/aes_ct_ctr.c
@@ -104,13 +104,23 @@ br_aes_ct_ctr_run(const br_aes_ct_ctr_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_ct_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_ct_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_ct_ctr_vtable = {
 	sizeof(br_aes_ct_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_ct_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_ct_ctr_run
+	&br_aes_ct_ctr_init_wrapper,
+	&br_aes_ct_ctr_run_wrapper
 };
diff --git a/src/symcipher/aes_ct_ctrcbc.c b/src/symcipher/aes_ct_ctrcbc.c
index 8ae9fc7..80a0dc4 100644
--- a/src/symcipher/aes_ct_ctrcbc.c
+++ b/src/symcipher/aes_ct_ctrcbc.c
@@ -401,22 +401,47 @@ br_aes_ct_ctrcbc_decrypt(const br_aes_ct_ctrcbc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_ct_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_ct_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_ct_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_ct_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_ct_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_ct_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_ct_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_ct_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_ct_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_ct_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_ct_ctrcbc_vtable = {
 	sizeof(br_aes_ct_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_ct_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_ct_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_ct_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_ct_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_ct_ctrcbc_mac
+	&br_aes_ct_ctrcbc_init_wrapper,
+	&br_aes_ct_ctrcbc_encrypt_wrapper,
+	&br_aes_ct_ctrcbc_decrypt_wrapper,
+	&br_aes_ct_ctrcbc_ctr_wrapper,
+	&br_aes_ct_ctrcbc_mac_wrapper
 };
diff --git a/src/symcipher/aes_pwr8_cbcdec.c b/src/symcipher/aes_pwr8_cbcdec.c
index e535ba6..5987ac8 100644
--- a/src/symcipher/aes_pwr8_cbcdec.c
+++ b/src/symcipher/aes_pwr8_cbcdec.c
@@ -641,14 +641,24 @@ br_aes_pwr8_cbcdec_run(const br_aes_pwr8_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_pwr8_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_pwr8_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_pwr8_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_pwr8_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_pwr8_cbcdec_vtable = {
 	sizeof(br_aes_pwr8_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_pwr8_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_pwr8_cbcdec_run
+	&br_aes_pwr8_cbcdec_init_wrapper,
+	&br_aes_pwr8_cbcdec_run_wrapper
 };
 
 /* see bearssl_block.h */
diff --git a/src/symcipher/aes_pwr8_cbcenc.c b/src/symcipher/aes_pwr8_cbcenc.c
index 00f8eca..2078440 100644
--- a/src/symcipher/aes_pwr8_cbcenc.c
+++ b/src/symcipher/aes_pwr8_cbcenc.c
@@ -388,14 +388,24 @@ br_aes_pwr8_cbcenc_run(const br_aes_pwr8_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_pwr8_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_pwr8_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_pwr8_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_pwr8_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_pwr8_cbcenc_vtable = {
 	sizeof(br_aes_pwr8_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_pwr8_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_pwr8_cbcenc_run
+	&br_aes_pwr8_cbcenc_init_wrapper,
+	&br_aes_pwr8_cbcenc_run_wrapper
 };
 
 /* see bearssl_block.h */
diff --git a/src/symcipher/aes_pwr8_ctr.c b/src/symcipher/aes_pwr8_ctr.c
index f5d20c0..0b31a2b 100644
--- a/src/symcipher/aes_pwr8_ctr.c
+++ b/src/symcipher/aes_pwr8_ctr.c
@@ -687,15 +687,25 @@ br_aes_pwr8_ctr_run(const br_aes_pwr8_ctr_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_pwr8_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_pwr8_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_pwr8_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_pwr8_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_pwr8_ctr_vtable = {
 	sizeof(br_aes_pwr8_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_pwr8_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_pwr8_ctr_run
+	&br_aes_pwr8_ctr_init_wrapper,
+	&br_aes_pwr8_ctr_run_wrapper
 };
 
 /* see bearssl_block.h */
diff --git a/src/symcipher/aes_pwr8_ctrcbc.c b/src/symcipher/aes_pwr8_ctrcbc.c
index a67d30b..6f53325 100644
--- a/src/symcipher/aes_pwr8_ctrcbc.c
+++ b/src/symcipher/aes_pwr8_ctrcbc.c
@@ -914,24 +914,49 @@ br_aes_pwr8_ctrcbc_mac(const br_aes_pwr8_ctrcbc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_pwr8_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_pwr8_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_pwr8_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_pwr8_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_pwr8_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_pwr8_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_pwr8_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_pwr8_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_pwr8_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_pwr8_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_pwr8_ctrcbc_vtable = {
 	sizeof(br_aes_pwr8_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_pwr8_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_pwr8_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_pwr8_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_pwr8_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_pwr8_ctrcbc_mac
+	&br_aes_pwr8_ctrcbc_init_wrapper,
+	&br_aes_pwr8_ctrcbc_encrypt_wrapper,
+	&br_aes_pwr8_ctrcbc_decrypt_wrapper,
+	&br_aes_pwr8_ctrcbc_ctr_wrapper,
+	&br_aes_pwr8_ctrcbc_mac_wrapper
 };
 
 #else
diff --git a/src/symcipher/aes_small_cbcdec.c b/src/symcipher/aes_small_cbcdec.c
index 8567244..9e3e404 100644
--- a/src/symcipher/aes_small_cbcdec.c
+++ b/src/symcipher/aes_small_cbcdec.c
@@ -58,12 +58,22 @@ br_aes_small_cbcdec_run(const br_aes_small_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_small_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_small_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_small_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_small_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_small_cbcdec_vtable = {
 	sizeof(br_aes_small_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_small_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_small_cbcdec_run
+	&br_aes_small_cbcdec_init_wrapper,
+	&br_aes_small_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/aes_small_cbcenc.c b/src/symcipher/aes_small_cbcenc.c
index 0dc2910..9cc19c0 100644
--- a/src/symcipher/aes_small_cbcenc.c
+++ b/src/symcipher/aes_small_cbcenc.c
@@ -56,12 +56,22 @@ br_aes_small_cbcenc_run(const br_aes_small_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_small_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_small_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_small_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_small_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_small_cbcenc_vtable = {
 	sizeof(br_aes_small_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_small_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_small_cbcenc_run
+	&br_aes_small_cbcenc_init_wrapper,
+	&br_aes_small_cbcenc_run_wrapper
 };
diff --git a/src/symcipher/aes_small_ctr.c b/src/symcipher/aes_small_ctr.c
index d5d371c..896b52d 100644
--- a/src/symcipher/aes_small_ctr.c
+++ b/src/symcipher/aes_small_ctr.c
@@ -72,13 +72,23 @@ br_aes_small_ctr_run(const br_aes_small_ctr_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_small_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_small_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_small_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_small_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_small_ctr_vtable = {
 	sizeof(br_aes_small_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_small_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_small_ctr_run
+	&br_aes_small_ctr_init_wrapper,
+	&br_aes_small_ctr_run_wrapper
 };
diff --git a/src/symcipher/aes_small_ctrcbc.c b/src/symcipher/aes_small_ctrcbc.c
index 2d6ba32..e95b922 100644
--- a/src/symcipher/aes_small_ctrcbc.c
+++ b/src/symcipher/aes_small_ctrcbc.c
@@ -121,22 +121,47 @@ br_aes_small_ctrcbc_decrypt(const br_aes_small_ctrcbc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_aes_small_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_small_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_small_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_small_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_small_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_small_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_small_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_small_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_small_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_small_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_small_ctrcbc_vtable = {
 	sizeof(br_aes_small_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_small_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_small_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_small_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_small_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_small_ctrcbc_mac
+	&br_aes_small_ctrcbc_init_wrapper,
+	&br_aes_small_ctrcbc_encrypt_wrapper,
+	&br_aes_small_ctrcbc_decrypt_wrapper,
+	&br_aes_small_ctrcbc_ctr_wrapper,
+	&br_aes_small_ctrcbc_mac_wrapper
 };
diff --git a/src/symcipher/aes_x86ni_cbcdec.c b/src/symcipher/aes_x86ni_cbcdec.c
index 862b1b5..d1a2c4f 100644
--- a/src/symcipher/aes_x86ni_cbcdec.c
+++ b/src/symcipher/aes_x86ni_cbcdec.c
@@ -201,14 +201,24 @@ br_aes_x86ni_cbcdec_run(const br_aes_x86ni_cbcdec_keys *ctx,
 BR_TARGETS_X86_DOWN
 
 /* see bearssl_block.h */
+static void
+br_aes_x86ni_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_x86ni_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_x86ni_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_x86ni_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_aes_x86ni_cbcdec_vtable = {
 	sizeof(br_aes_x86ni_cbcdec_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_aes_x86ni_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_aes_x86ni_cbcdec_run
+	&br_aes_x86ni_cbcdec_init_wrapper,
+	&br_aes_x86ni_cbcdec_run_wrapper
 };
 
 #else
diff --git a/src/symcipher/aes_x86ni_cbcenc.c b/src/symcipher/aes_x86ni_cbcenc.c
index 85feecd..c3b231f 100644
--- a/src/symcipher/aes_x86ni_cbcenc.c
+++ b/src/symcipher/aes_x86ni_cbcenc.c
@@ -100,14 +100,24 @@ br_aes_x86ni_cbcenc_run(const br_aes_x86ni_cbcenc_keys *ctx,
 BR_TARGETS_X86_DOWN
 
 /* see bearssl_block.h */
+static void
+br_aes_x86ni_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_x86ni_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_x86ni_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_aes_x86ni_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_aes_x86ni_cbcenc_vtable = {
 	sizeof(br_aes_x86ni_cbcenc_keys),
 	16,
 	4,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_aes_x86ni_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_aes_x86ni_cbcenc_run
+	&br_aes_x86ni_cbcenc_init_wrapper,
+	&br_aes_x86ni_cbcenc_run_wrapper
 };
 
 #else
diff --git a/src/symcipher/aes_x86ni_ctr.c b/src/symcipher/aes_x86ni_ctr.c
index 1cddd60..64fe3fe 100644
--- a/src/symcipher/aes_x86ni_ctr.c
+++ b/src/symcipher/aes_x86ni_ctr.c
@@ -188,15 +188,25 @@ br_aes_x86ni_ctr_run(const br_aes_x86ni_ctr_keys *ctx,
 BR_TARGETS_X86_DOWN
 
 /* see bearssl_block.h */
+static void
+br_aes_x86ni_ctr_init_wrapper(const br_block_ctr_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_x86ni_ctr_init((void *)a0, a1, a2);
+}
+
+static uint32_t
+br_aes_x86ni_ctr_run_wrapper(const br_block_ctr_class *const * a0,
+		const void * a1, uint32_t a2, void * a3, size_t a4)
+{
+	return br_aes_x86ni_ctr_run((void *)a0, a1, a2, a3, a4);
+}
+
 const br_block_ctr_class br_aes_x86ni_ctr_vtable = {
 	sizeof(br_aes_x86ni_ctr_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctr_class **, const void *, size_t))
-		&br_aes_x86ni_ctr_init,
-	(uint32_t (*)(const br_block_ctr_class *const *,
-		const void *, uint32_t, void *, size_t))
-		&br_aes_x86ni_ctr_run
+	&br_aes_x86ni_ctr_init_wrapper,
+	&br_aes_x86ni_ctr_run_wrapper
 };
 
 #else
diff --git a/src/symcipher/aes_x86ni_ctrcbc.c b/src/symcipher/aes_x86ni_ctrcbc.c
index f57fead..015968e 100644
--- a/src/symcipher/aes_x86ni_ctrcbc.c
+++ b/src/symcipher/aes_x86ni_ctrcbc.c
@@ -564,24 +564,49 @@ br_aes_x86ni_ctrcbc_decrypt(const br_aes_x86ni_ctrcbc_keys *ctx,
 BR_TARGETS_X86_DOWN
 
 /* see bearssl_block.h */
+static void
+br_aes_x86ni_ctrcbc_init_wrapper(const br_block_ctrcbc_class ** a0, const void * a1, size_t a2)
+{
+	br_aes_x86ni_ctrcbc_init((void *)a0, a1, a2);
+}
+
+static void
+br_aes_x86ni_ctrcbc_encrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_x86ni_ctrcbc_encrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_x86ni_ctrcbc_decrypt_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, void * a3, size_t a4)
+{
+	br_aes_x86ni_ctrcbc_decrypt((void *)a0, a1, a2, a3, a4);
+}
+
+static void
+br_aes_x86ni_ctrcbc_ctr_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, void * a2, size_t a3)
+{
+	br_aes_x86ni_ctrcbc_ctr((void *)a0, a1, a2, a3);
+}
+
+static void
+br_aes_x86ni_ctrcbc_mac_wrapper(const br_block_ctrcbc_class *const * a0,
+		void * a1, const void * a2, size_t a3)
+{
+	br_aes_x86ni_ctrcbc_mac((void *)a0, a1, a2, a3);
+}
+
 const br_block_ctrcbc_class br_aes_x86ni_ctrcbc_vtable = {
 	sizeof(br_aes_x86ni_ctrcbc_keys),
 	16,
 	4,
-	(void (*)(const br_block_ctrcbc_class **, const void *, size_t))
-		&br_aes_x86ni_ctrcbc_init,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_x86ni_ctrcbc_encrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, void *, size_t))
-		&br_aes_x86ni_ctrcbc_decrypt,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, void *, size_t))
-		&br_aes_x86ni_ctrcbc_ctr,
-	(void (*)(const br_block_ctrcbc_class *const *,
-		void *, const void *, size_t))
-		&br_aes_x86ni_ctrcbc_mac
+	&br_aes_x86ni_ctrcbc_init_wrapper,
+	&br_aes_x86ni_ctrcbc_encrypt_wrapper,
+	&br_aes_x86ni_ctrcbc_decrypt_wrapper,
+	&br_aes_x86ni_ctrcbc_ctr_wrapper,
+	&br_aes_x86ni_ctrcbc_mac_wrapper
 };
 
 #else
diff --git a/src/symcipher/des_ct_cbcdec.c b/src/symcipher/des_ct_cbcdec.c
index d208a3d..62b5d77 100644
--- a/src/symcipher/des_ct_cbcdec.c
+++ b/src/symcipher/des_ct_cbcdec.c
@@ -76,12 +76,22 @@ br_des_ct_cbcdec_run(const br_des_ct_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_des_ct_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_des_ct_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_des_ct_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_des_ct_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_des_ct_cbcdec_vtable = {
 	sizeof(br_des_ct_cbcdec_keys),
 	8,
 	3,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_des_ct_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_des_ct_cbcdec_run
+	&br_des_ct_cbcdec_init_wrapper,
+	&br_des_ct_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/des_ct_cbcenc.c b/src/symcipher/des_ct_cbcenc.c
index 4b3610e..89831b5 100644
--- a/src/symcipher/des_ct_cbcenc.c
+++ b/src/symcipher/des_ct_cbcenc.c
@@ -58,12 +58,22 @@ br_des_ct_cbcenc_run(const br_des_ct_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_des_ct_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_des_ct_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_des_ct_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_des_ct_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_des_ct_cbcenc_vtable = {
 	sizeof(br_des_ct_cbcenc_keys),
 	8,
 	3,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_des_ct_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_des_ct_cbcenc_run
+	&br_des_ct_cbcenc_init_wrapper,
+	&br_des_ct_cbcenc_run_wrapper
 };
diff --git a/src/symcipher/des_tab_cbcdec.c b/src/symcipher/des_tab_cbcdec.c
index e7eabe9..a6aa6ff 100644
--- a/src/symcipher/des_tab_cbcdec.c
+++ b/src/symcipher/des_tab_cbcdec.c
@@ -74,12 +74,22 @@ br_des_tab_cbcdec_run(const br_des_tab_cbcdec_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_des_tab_cbcdec_init_wrapper(const br_block_cbcdec_class ** a0, const void * a1, size_t a2)
+{
+	br_des_tab_cbcdec_init((void *)a0, a1, a2);
+}
+
+static void
+br_des_tab_cbcdec_run_wrapper(const br_block_cbcdec_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_des_tab_cbcdec_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcdec_class br_des_tab_cbcdec_vtable = {
 	sizeof(br_des_tab_cbcdec_keys),
 	8,
 	3,
-	(void (*)(const br_block_cbcdec_class **, const void *, size_t))
-		&br_des_tab_cbcdec_init,
-	(void (*)(const br_block_cbcdec_class *const *, void *, void *, size_t))
-		&br_des_tab_cbcdec_run
+	&br_des_tab_cbcdec_init_wrapper,
+	&br_des_tab_cbcdec_run_wrapper
 };
diff --git a/src/symcipher/des_tab_cbcenc.c b/src/symcipher/des_tab_cbcenc.c
index 3a45ba3..8900b4e 100644
--- a/src/symcipher/des_tab_cbcenc.c
+++ b/src/symcipher/des_tab_cbcenc.c
@@ -56,12 +56,22 @@ br_des_tab_cbcenc_run(const br_des_tab_cbcenc_keys *ctx,
 }
 
 /* see bearssl_block.h */
+static void
+br_des_tab_cbcenc_init_wrapper(const br_block_cbcenc_class ** a0, const void * a1, size_t a2)
+{
+	br_des_tab_cbcenc_init((void *)a0, a1, a2);
+}
+
+static void
+br_des_tab_cbcenc_run_wrapper(const br_block_cbcenc_class *const * a0, void * a1, void * a2, size_t a3)
+{
+	br_des_tab_cbcenc_run((void *)a0, a1, a2, a3);
+}
+
 const br_block_cbcenc_class br_des_tab_cbcenc_vtable = {
 	sizeof(br_des_tab_cbcenc_keys),
 	8,
 	3,
-	(void (*)(const br_block_cbcenc_class **, const void *, size_t))
-		&br_des_tab_cbcenc_init,
-	(void (*)(const br_block_cbcenc_class *const *, void *, void *, size_t))
-		&br_des_tab_cbcenc_run
+	&br_des_tab_cbcenc_init_wrapper,
+	&br_des_tab_cbcenc_run_wrapper
 };
